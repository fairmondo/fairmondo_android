package de.handler.mobile.android.fairmondo.data.interfaces;

/**
 * Informs the implementing class that the user want to retry the last action - now with internet.
 */
public interface OnNetworkAvailableListener {
    void onNetworkAvailable();
}
