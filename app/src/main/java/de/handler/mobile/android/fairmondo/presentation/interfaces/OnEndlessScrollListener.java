package de.handler.mobile.android.fairmondo.presentation.interfaces;

/**
 * Informs the implementing class that the end of a list has been reached.
 */
public interface OnEndlessScrollListener {
    void onListEndReached(int pageCounter);
}
